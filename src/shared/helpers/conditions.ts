import { useContext } from "react"
import Contexts, { IContext } from "../contexts"

export const conditions = (
    player: "p1" | "p2",
    choice: "cheese" | "circle",
    bloco: [0,1,2,3,4,5,6,7,8]
) => {

    // [0, 1, 2]
}